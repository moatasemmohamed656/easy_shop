import 'package:flutter/material.dart';

class AddNotifications extends StatelessWidget {
  const AddNotifications({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: 
        Text('AddNotifications'),
      ),
    );
  }
}